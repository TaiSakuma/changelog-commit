{
  "name": "changelog-commit",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/patch-config.ts --bundle --platform=node --format=cjs --outfile=dist/patch-config.cjs",
    "test": "vitest run"
  },
  "dependencies": {
    "smol-toml": "^1.3.1"
  },
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "fast-check": "^4.0.0",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
